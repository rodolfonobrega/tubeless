# ────────────────────────────────────────────────────────────────
# Backend dependencies — single source of truth for pip installs.
# Used by:
#   • Dockerfile  →  pip install -r requirements.txt
#   • Local dev   →  pip install -r requirements.txt
#   • CI/CD       →  pip install -r requirements.txt
# ────────────────────────────────────────────────────────────────

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.25
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
alembic>=1.13.1
pgvector>=0.2.1

# Config / validation
pydantic>=2.5.3
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# LLM providers
litellm>=1.35.0
openai>=1.10.0
anthropic>=0.18.0

# YouTube
yt-dlp>=2024.1.0
youtube-transcript-api>=1.0.0

# HTTP / networking
httpx>=0.26.0
websockets>=12.0

# AI / ML utilities
tiktoken>=0.5.2
flashrank>=0.2.9

# Observability
structlog>=24.4.0

# Browser automation (needed for YouTube scraping fallback)
playwright>=1.40.0
